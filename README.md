# 🎵 Аудиоанализатор с OpenAI API

Интеллектуальное Python-приложение для анализа аудиофайлов с использованием OpenAI API. Поддерживает высококачественное распознавание речи, извлечение вокала и ИИ-классификацию аудио.

## 🚀 Возможности

- 🔍 **Поиск аудиофайлов** — автоматически находит все аудиофайлы в указанной папке (исключая `*_vocals.*`)
- 🎵 **Поддержка форматов** — MP3, WAV, M4A, FLAC
- 🎙️ **Извлечение вокала** — Demucs (при наличии) или упрощённое выделение диапазона голосов через librosa; сохранение `{имя}_vocals.wav` рядом с исходником
- 📝 **Распознавание речи** — GPT-4o-transcribe, многошаговый алгоритм с приоритетом русского языка
- 🤖 **ИИ-классификация** — GPT-4o-mini на основе транскрипта + эвристики (инструментальная музыка, вокализации, шум)
- 🛡️ **Фильтрация галлюцинаций** — удаление системного промпта и явных шумовых шаблонов
- 🧠 **Пост-обработка (опционально)** — LLM-коррекция транскрипта и лёгкая нормализация пунктуации/идиом для RU
- 🌍 **Многоязычность** — приоритет русского, альтернативный английский
- 📦 **Пакетная обработка** — анализ всех файлов в папке
- 💾 **Сохранение результатов** — `result_{имя_файла}.txt` для одиночного запуска; `result_{время}.txt` для пакетного
- 📝 **Логирование** — подробные логи сессий в папке `log/` (можно отключить)

## 📦 Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/yuristep/ai_transcribe.git
cd ai_transcribe
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
# Linux/Mac
source venv/bin/activate
# Windows
venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Получите API ключ OpenAI: `https://platform.openai.com/api-keys`

5. Настройте переменные окружения (.env в корне, приоритетный)
```bash
# пример
copy examples\env_example.txt .env   # Windows
# или
cp examples/env_example.txt .env      # Linux/Mac
# затем впишите OPENAI_API_KEY
```

Содержимое .env:
```
OPENAI_API_KEY=your_openai_api_key_here
```

Примечание: приложение работает даже при отсутствии некоторых необязательных зависимостей (например, без `pydub` и Demucs используется упрощённый путь).

## 🚀 Быстрый старт

```bash
# Интерактивный выбор файла из папки
python main.py examples

# Пакетный анализ всех файлов
python main.py --all-files examples

# Анализ конкретного файла
python main.py --file examples/song.mp3

# С отладкой и логами
python main.py --all-files examples --debug

# Без сохранения результатов
python main.py --all-files examples --no-save

# Включить LLM пост-обработку
python main.py --file examples/song.mp3 --post-process

# Быстрый режим (без улучшения аудио)
python main.py --file examples/song.mp3 --fast-mode

# Отключить логирование в файл
python main.py --file examples/song.mp3 --no-logging
```

## 📁 Структура проекта
```
ai_transcribe/
├── main.py                 # Основной файл приложения
├── requirements.txt        # Зависимости Python
├── README.md               # Документация
├── .env                    # Переменные окружения (создайте из examples/env_example.txt)
├── create_test_files.py    # Скрипт для генерации тестовых аудио
└── examples/               # Примеры аудиофайлов и .env-шаблон
```

## 🎯 Использование

### Параметры командной строки (актуально)

| Параметр | Описание | Пример |
|---|---|---|
| `--all-files` | Пакетное распознавание всех аудиофайлов в папке | `--all-files examples` |
| `--file <путь>` | Анализ конкретного файла | `--file examples/song.mp3` |
| `--save` / `--no-save` | Вкл/выкл сохранения результатов | `--no-save` |
| `--debug` | Подробные логи и запросы/ответы | `--debug` |
| `--primary-lang <язык>` | Основной язык (по умолчанию: ru) | `--primary-lang en` |
| `--secondary-lang <язык>` | Альтернативный язык (по умолчанию: en) | `--secondary-lang ru` |
| `--post-process` | Включить LLM-постобработку транскрипта | `--post-process` |
| `--no-logging` | Отключить логирование в файл | `--no-logging` |
| `--enable-post-process` | Включить пост-обработку по умолчанию | `--enable-post-process` |
| `--fast-mode` | Быстрый режим (без улучшения аудио) | `--fast-mode` |

### Сохранение результатов
- Один файл → `result_{имя_файла}.txt`
- Пакетная обработка → `result_{время}.txt`
- Вокал → `{имя}_vocals.wav` рядом с исходником (перезаписывается)
- В пакетном режиме файлы `*_vocals.*` пропускаются, чтобы не зациклиться

## 🔧 Режимы улучшения/разделения (актуально)

Настройка параметров Demucs выполняется только в блоке конфигурации `main.py` (CLI‑флагов для выбора модели/качества нет). Используется системный FFmpeg; расширения torio FFmpeg отключены и не влияют на качество разделения.

Рекомендуемые пресеты (меняются в `main.py`):

- Fast (максимальная скорость)
```python
ENABLE_AUDIO_ENHANCEMENT = False  # или запуск с --fast-mode
```

- Balanced (по умолчанию)
```python
DEMUCS_MODEL = "htdemucs"
DEMUCS_SHIFTS = 4
DEMUCS_OVERLAP = 0.4
DEMUCS_JOBS = 4
DEMUCS_TWO_STEMS = "vocals"
```

- High‑quality (лучше подавление подмешивания музыки)
```python
DEMUCS_MODEL = "htdemucs"
DEMUCS_SHIFTS = 6  # 6–8
DEMUCS_OVERLAP = 0.5
DEMUCS_JOBS = 4
DEMUCS_TWO_STEMS = "vocals"
```

При ошибке Demucs автоматически используется упрощённый fallback через librosa.

## 🔧 Технические детали

### Предобработка/улучшение аудио
1) Предочистка (лёгкий HPF/эквализация) для улучшения разделения
2) Разделение стемов Demucs (`--two-stems vocals`) при наличии; иначе — упрощённое выделение голоса через librosa
3) Мягкое вычитание фона из вокала и лёгкая пост-очистка
4) Сохранение `{stem}_vocals.wav`

Можно отключить улучшение аудио параметром `--fast-mode` (ускоряет обработку).

### Транскрипция (GPT-4o-transcribe)
- Попытка 1: основной язык (ru) на улучшенном файле (если есть)
- Попытка 2: авто-язык, если результат слишком короткий
- Попытка 3: музыкальный промпт — только если обнаружены музыкальные признаки
- Фильтрация: удаление системного промпта и явных шумовых артефактов
- (Опционально) LLM-постобработка + лёгкая нормализация пунктуации/идиом (RU)

### Классификация (GPT-4o-mini)
- Вход: готовый транскрипт, без повторного STT
- Строгий системный промпт с примерами типовых шумов (город, машины, вода, ветер, механические)
- Эвристики: инструментальная музыка (ритм/тональность), вокализации (la/na/ah…), явный шум по тексту

## 📊 Пример вывода
```
==================================================
📊 РЕЗУЛЬТАТЫ АНАЛИЗА
==================================================
📁 Файл: song.mp3
🏷️ Тип аудио: музыка
📝 Распознанный текст:
   Позвони мне. Не говори нет. Чё ты гонишь? Снова мозг мне выносишь.
==================================================
```

## 🐛 Отладка
```bash
python main.py --file examples/song.mp3 --debug
```
Отладка включает параметры запуска, детальные запросы/ответы к OpenAI, логи предобработки/разделения, фильтрацию, классификацию. Логи сохраняются в `log/` (можно отключить `--no-logging`).

## ⚠️ Ограничения
- Интернет и действующий OpenAI API ключ обязательны
- Лимиты на размер файлов согласно OpenAI
- Качество распознавания зависит от качества исходника и качества разделения вокала

## 🤝 Вклад
1. Форк, ветка, изменения, PR

## 📄 Лицензия
MIT